<body>
  <img class="logo" src="/images/pusheen.png">
  <span class="header"><strong>Pusheen's Gambling Den</span></strong>
  <div>
    <% if(!player_id){ %>
      <h3><form action="/login" method="POST">
      <h5 class="text1">You are not currently logged in.</h5>
<textarea id="player" name="player" placeholder="Enter username!"></textarea>
      <button class="button1"><span>Login</span></button></form>
      <form  action="/login" method="GET" >
      </form>
      <% } else{ %>
    </h3>
      <h3 class="text"> Welcome, you are now logged in!</h3>
      <% } %>
  </div>
</body>
<!-- <button class="login"><span>Login</span></button>
    <h6> Last login: tempDate </h6>
    <textarea id="login-input" name="text" placeholder="Enter username!"></textarea>
    <button class="logout"><span>Logout</span></button>
  </div> -->
